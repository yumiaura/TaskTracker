# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# Tooling caches
.mypy_cache/
.ruff_cache/
.pytest_cache/

# What `pytest --cov` leaves behind.
.coverage
.coverage.*
htmlcov/
coverage.xml

# Local settings for docker compose - .env.example is the one in the history.
.env

# The tracker's own data, if anybody points TASKTRACKER_HOME at the checkout
# while developing. The real store lives under ~/.claude/tasktracker and is
# never inside this tree, but a stray tasks.db committed once is a stray
# tasks.db in the history for good.
*.db
*.db-wal
*.db-shm

# Editors
.idea/
.vscode/
*.swp
